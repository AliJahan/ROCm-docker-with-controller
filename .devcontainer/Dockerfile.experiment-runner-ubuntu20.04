FROM gpu-workloads-remote-rocm-ubuntu:latest
LABEL maintainer=ajaha004@ucr.edu

ARG CONTROL_BROAD_CAST_PORT="45678"

ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get -qq update && apt-get -qq install -y  xxd libzmq3-dev python3 python3-pip iproute2

COPY ../scripts/utils/experiment_runner /experiment_runner

RUN cd /experiment_runner \
    && pip3 install -f requirements.txt

